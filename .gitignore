# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dev-dist
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Local Claude Code settings (launch.json is shared, settings.local.json isn't)
.claude/settings.local.json

# Local-only Claude Code project context (not shared with the team repo)
CLAUDE.md
project_specs.md

# Local agent tooling — installed per-developer by `npx convex ai-files install`
# and the skills installer. Not part of the app build; regenerate with that
# command rather than committing 80+ generated skill files.
.agents/
.claude/skills/
AGENTS.md
skills-lock.json
convex/_generated/ai/
convex/tsconfig.json
